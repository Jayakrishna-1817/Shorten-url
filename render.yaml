services:
  # Single Node.js service serving both API and frontend
  - type: web
    name: url-shortener-combined
    env: node
    buildCommand: npm install && npm run build && cd server && npm install
    startCommand: cd server && node server.js
    healthCheckPath: /health
